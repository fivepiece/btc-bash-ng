#!/usr/bin/bc

# some basic useful functions

define max(a, b){

    if ( a > b ){
        return a
    } else {
        return b
    }
}

define min(a, b){

    if ( a < b ){
        return a
    } else {
        return b
    }
}
